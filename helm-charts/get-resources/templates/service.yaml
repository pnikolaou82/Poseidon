apiVersion: v1
kind: Service
metadata:
  name: {{ include "get-resources.fullname" . }}
  labels:
    {{- include "get-resources.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "get-resources.selectorLabels" . | nindent 4 }}

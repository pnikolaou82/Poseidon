piVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: prometheusruletuners.sre.taikun.cloud
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
spec:
  group: sre.taikun.cloud
  names:
    plural: prometheusruletuners
    singular: prometheusruletuner
    kind: PrometheusRuleTuner
    listKind: PrometheusRuleTunerList
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              type: object
              properties:
                deploymentName:
                  type: string
                  description: Name of the deployment to monitor
                namespace:
                  type: string
                  description: Namespace of the deployment
            status:
              type: object
              properties:
                cpuUsage:
                  type: string
                  description: Current CPU usage (e.g., "250m")
                memoryUsage:
                  type: string
                  description: Current memory usage (e.g., "128Mi")
      subresources:
        status: {}
